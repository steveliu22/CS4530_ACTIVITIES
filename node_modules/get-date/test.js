'use strict';
var test = require('ava');
var dateTime = require('./');

test('return a date', function (t) {
  t.assert(dateTime() !== undefined);
  t.end();
});

test('return a date & time', function (t) {
  t.assert(dateTime(true) !== undefined);
  t.end();
});